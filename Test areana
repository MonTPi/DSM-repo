def main():
     x = get_int()
     print(f"x is {x}")

def get_int():
    while True:
        try:
            return int(input('what is number? '))
        except ValueError:
#Soft nudge rather than error message: pass (begins the next loop)
            pass

        
main()

